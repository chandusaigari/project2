apiVersion: v1
kind: Service
metadata:
  name: flask-backend-service
  namespace: e-ns 
spec:
  type: NodePort
  selector:
    app: flask-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30002
